Learn more about cpp.

## C++ source file organization

- Compile programs using c++11 in `cpp11/` folder. Other folders are similar.
- In the simplest case, each c++ source file is compiled into an executable, and each source file need to have a unique name, due to the logic in the CMakeLists file.
